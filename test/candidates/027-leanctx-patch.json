{
  "id": "cand-027",
  "trigger_pattern": "Apply a patch via LeanCTX",
  "keywords": ["patch", "replace", "edit", "write", "leanctx"],
  "goal": "Use the LeanCTX ctx_patch tool to replace the text 'return a + b;' with 'return a + b + 1;' in src/main.cpp",
  "context": "The sandbox is the project root for the LeanCTX session; src/main.cpp contains 'return a + b;' inside add(); the local LLM has the LeanCTX ctx_patch tool",
  "constraints": [
    "Use ONLY the LeanCTX ctx_patch tool",
    "Provide the exact parameters: {op, path, find, replace}",
    "op = 'replace_unique', path = 'src/main.cpp', find = 'return a + b;', replace = 'return a + b + 1;'"
  ],
  "tool_template": {
    "service": "leanctx",
    "tool": "ctx_patch",
    "syntax": "{ \"op\": \"<op>\", \"path\": \"<path>\", \"find\": \"<text>\", \"replace\": \"<text>\" }"
  },
  "fixture": "test/fixtures/serena-project",
  "steps": [
    { "service": "leanctx", "tool": "ctx_patch", "args": { "op": "replace_unique", "path": "src/main.cpp", "find": "return a + b;", "replace": "return a + b + 1;" } }
  ],
  "source_conversation_id": "manual-leanctx-patch",
  "difficulty": "hard",
  "pass_fail": {
    "type": "contains",
    "path": "src/main.cpp",
    "needle": "a + b + 1",
    "expected": "src/main.cpp contains the replaced text 'a + b + 1'"
  }
}
