# Dependencies
node_modules/

# Build output
dist/

# Local state (Cadet Brainstem)
.cadet-brainstem/
.cadet/

# Serena local project state (created on activation)
.serena/

# SQLite metrics databases
*.db
*.sqlite
*.sqlite3

# Test sandbox artifacts (candidate run scratch dirs — cleaned after each run)
test/artifacts/

# npm pack output
*.tgz

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store

# Stray symbol-name files accidentally created by a broken rename codemod
# (task 59). Ignored so they can never be re-added to the repo.
OllamaSteerer,
steerOrDegrade,
steerTool,
steerWithFallback,
steering
getSteeringCallsByOrigin
Thumbs.db
.vscode/*
!.vscode/extensions.json
!.vscode/mcp.json
!.vscode/tasks.json
